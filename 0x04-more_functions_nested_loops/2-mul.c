#include "main.h"

/**
 * mul - a function that multiplies two integers.
 * @a: the  numbers being added
 * @b: the  numbers being added
 * Return: the product
*/
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
