#include "main.h"

/**
 * _abs - prints absolute value of number
 * @n: the integer value passed
 * Return: the absolute value of n
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
